#version: '3.1'
services:
  wos245-app:
    build: ./docker
    image: leafphp/docker
    ports:
      - '80:80'
    volumes:
      - .:/var/www
      # For SQlite database, could mount different dir from host OS
      #- type: bind
      #  source: /var/local/wos245/
      #  target: /var/www/wos245/
